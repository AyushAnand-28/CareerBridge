# ─── Backend API URL ──────────────────────────────────────────────────────────
# Set this to your deployed backend URL (Render URL or custom domain).
# In development this is left empty — Vite's dev proxy handles /api calls locally.
# In production this MUST point to your live backend.
VITE_API_URL="https://careerbridge-api.onrender.com"
